Lecture 2:
__________________
1. #include <iostream>
   using namespace std;
   int main() 
   {                                    Output: 0
    int var1 = 5;                       
    int var2 = 6;
    cout << (var1 > var2);
   }
2.   #include <iostream>
     using namespace std;               Output: careerlabs
     int main(){
    cout << "career" << "labs";
    }
3.  Which of the following data type stores longest decimal number ?           Answer : double
4.  Compiler assigns a garbage value to an uninitialised local variable in C++ Programming ?  Answer : true  

5.  bool a = true;
    cout << a;                          Output: 1
6.What is the output of the following code if the input is : 5 15?

  #include <iostream>
  using namespace std;
  int main() {
        int a, b; 
    cin >> a;                           Output: 20       
    cin >> b;
    cout << (a+b);
            return 0;
  }
7. What is the output of the following code if the input is : 2 10.1 D ?
#include <iostream>
using namespace std;
int main() {
    int a;                              Output: 210.1D
    double d;
    char c;
    cin >> a >> d >> c;
    cout << a << d << c << endl;
}
8. What will be the output of following statement ?
 cout << (‘a’ + 1);                     Output: 98  It will add the ASCII value of  char ‘a’ i.e 97  and int 1. So answer will be 98.
9. What will be the output of following statement ?
  int i = 'c';
  cout << i;                            Output: 99  When we put char 'c' into an integer, its ASCII value will be put in the integer i.e. 99.
10. What is the output ?
#include <iostream>
using namespace std;
int main()
{                                       Output: J  When we assign an integer to a character variable, that integer will be treated as ASCII value of corresponding character. 74 is ASCII value of letter 'J'. Hence, 'J' will be printed.
       char c = 74;
       cout << c;
       return 0;
} 
11. What is the output ?
#include <iostream>
using namespace std;
int main()
{                                      Output: k   "ch + a" will give the answer 107 (ASCII value of 'a' is 97). But we are assigning this integer now to the character variable. Hence, 107 will be saved in the variable "ch" and on printing it will print the character which is having ASCII value 107 i.e. 'k'. 

    int a = 10;
    char ch = 'a';
    ch = ch + a;
    cout << ch << endl;
}
12. What will be the output ?
#include <iostream>
using namespace std;   
int main()
{                                      Output: 2  When 6 / 4 is performed, both the operands of / are int hence answer will be an int i.e. 1. Hence the value of both a and b is 1. Thus a + b will be 2. In C++, if a double value doesn't contain any decimal value, only integer will be printed.
    double a = 6 / 4;
    int b  = 6 / 4;
    double c = a + b;
    cout << c;
}
13. #include <iostream>
using namespace std;  
int main()
{                                     Output: Syntax error .% can’t be used with double or float.
    double a = 55.5;
    int b = 55;
    a = a % 10;
    b = b % 10;
    cout << a << " "  << b;
}
14. #include <iostream>
using namespace std;
int main() 
{                                    Output: 0 .'>' operator gives a boolean answer. And the condition is false. So answer will be false and 0 will be printed (boolean variables print only 0 and 1).
    int var1 = 5;
    int var2 = 6;
    cout << (var1 > var2);
 }
